/**
 * DigiGram — লেআউট ও সেকশন আইডি (JS থেকে ইমপোর্ট করে ব্যবহার করুন)।
 * রঙ/গ্রেডিয়েন্ট: `styles/digigram-theme.css` এ `--dg-*` ভেরিয়েবল।
 */

export const layout = {
    maxWidthPx: 1440,
    servicesMaxPx: 1200,
};

export const sectionIds = {
    services: 'services',
};

export const themeFiles = {
    css: 'styles/digigram-theme.css',
};

export { paths } from '@/lib/constants/paths';
